# CLAUDE.md — 이 저장소에서 일하는 Claude 지침

이 파일은 **Claude**의 세션 시작 지침이다. Codex는 `AGENTS.md`를 읽는다.
두 파일은 **같은 governance 정본**을 가리키며, 규칙이 다르면 governance 정본이 우선한다.

---

## 1. 시작 잠금 — 아래를 하기 전에는 어떤 작업도 착수하지 않는다

**필수 읽기 순서 (4종, 이 순서대로)**

1. `docs/governance/CONSTITUTION.md` — 무엇을 해도 되고 안 되는가
2. `docs/governance/MASTER_PLAN.md` — 지금 어느 단계이고 다음 한 작업은 무엇인가
3. `docs/governance/CURRENT_STATE.md` — 지금 저장소·배포·데이터의 실제 상태
4. `docs/governance/DECISIONS.md` — 이미 정해진 것을 다시 뒤집지 않기 위해

**시작 잠금**: 작업 시작 보고의 **첫 줄**에 `MASTER_PLAN.md §2`의 **현재 단계와 다음 한 작업을 인용**한다.
인용하지 못하면 문서를 읽지 않은 것으로 간주하고 **착수하지 않는다**(헌법 §15).

**요청이 현재 단계 밖이면**: 착수 전에 그 사실을 사용자에게 먼저 말한다. 임의로 단계를 건너뛰지 않는다.

## 2. 직접 Git 관측 — 문서를 믿지 말고 저장소를 본다

매 작업 시작 시 **직접 실행해서** 확인한다. 이전 세션의 기억이나 다른 문서의 값을 인용하지 않는다.

```
git rev-parse --abbrev-ref HEAD
git rev-parse HEAD
git rev-parse main origin/main
git status --porcelain
```

`CURRENT_STATE.md`의 값과 다르면 **문서가 낡은 것**이다. 실제 관측값을 보고하고 문서를 갱신한다.

## 3. 완료보고 독립검증 의무

Codex의 작업 지시나 이전 완료 보고서를 **사실로 전제하지 않는다**(헌법 §12).

- 지시가 코드 구조나 사용자 목적과 충돌하면 **구현 전에 반박**한다
- 파일:행 주장은 **직접 열어** 확인한다
- "무회귀"는 `npm test` 실행 결과로만 주장한다
- 화면 기능은 **실제 진입 경로**로 확인한다 (코드 존재 ≠ 실무 작동, 헌법 §6)

## 4. 증거 규율 (헌법 §10)

**주장에는 확인 범위를 함께 쓴다.**

- ❌ "우회 없음" / "전체 무회귀" / "모든 화면 검증"
- ✅ "`src/services`와 `src/components`에서 검색해 0건" / "manifest 등록 120개 통과" / "Preview에서 흐름 A·B·C를 직접 실행"

확인하지 않은 경로는 **"미확인"**으로 남긴다. 파일 검색·실행 검사·브라우저 관측이 각각 무엇을 증명하는지 구분한다.

**존재 확인과 내용 검색을 한 명령에 섞지 않는다.** (`[ -f "$f" ] && grep -c ... || echo "없음"` 형태는 파일이 있어도 검색 0건이면 "없음"을 출력한다 — `DECISIONS.md` D-005 참조)

## 5. RED→GREEN 증거 (헌법 §10)

- 실제 제품 함수·경계를 호출하는 **실패 재현**을 먼저 만든다
- RED와 GREEN 커밋을 구분해 증거를 보존한다
- **문자열 존재 검사만으로 완료하지 않는다**
- 필요하면 음성 변형으로 검사가 실제 결함을 잡는지 확인한다

## 6. 검사 실행

```
npm test      # 전체 게이트: smoke(manifest) + build(tsc -b + typecheck:api + vite build) + lint
npm run smoke # smoke 구간만
```

- manifest: `scripts/regression-manifest.json`
- **현재 통과하는 검사를 사유 없이 제외하지 않는다.** 제외하려면 `exclude`에 `{ file, reason }`을 적어야 하고 러너가 그 목록을 출력한다
- 검사를 새로 추가하면 manifest `include`에 등록한다. 등록하지 않으면 러너가 비정상 종료한다
- 게이트 분할·검사 제외는 **파일명·이유·위험을 보고하고 별도 승인** 후에만 (`DECISIONS.md` D-003)

## 7. 하지 않는 것 (헌법 §13)

사용자의 **명시적 승인 없이** 하지 않는다:

- main 병합 · Production 배포 · 브랜치 삭제
- 데이터 삭제 · 환경변수 변경
- 외부 WRITE(고도몰 등록·고객 답글 발송·SNS 게시) · 결제
- `amend` · `rebase` · `force push`

인증 브랜치 `fix/auth-foundation-01-red`와 그 커밋은 **변경·병합·rebase·삭제하지 않는다**(`DECISIONS.md` D-002).

**침묵을 승인으로 해석하지 않는다**(헌법 §11).

## 8. 범위 규율 (헌법 §9)

- 한 번에 **하나의 종료조건 또는 하나의 결함**만 다룬다
- 착수 전에 성공 기준과 범위를 고정한다
- 도중 발견은 **현재 종료조건을 직접 막을 때만** 포함한다
- 나머지는 `MASTER_PLAN.md §14 후속 대장`에 기록하고 넘어간다
- **인프라를 요구사항보다 먼저 만들지 않는다**(헌법 §8)

## 9. 사용자 보고 형식 (헌법 §14)

기술 증거보다 **먼저** 다음 4가지를 쉬운 말로 쓴다.

1. 실제로 끝난 것
2. 사용자가 체감할 변화
3. 남은 것과 이번 범위 밖인 것
4. 사용자가 결정해야 할 것이 있는지

금지: 분모 없는 진행률 · 자동검사를 육안검증으로 표현 · Preview 결과를 Production 완료로 표현 · 과장된 완료

## 10. 이 저장소의 환경 메모

- Windows / PowerShell 기본, Bash 도구도 사용 가능
- 배포: Vercel (개발·검증·Production **모두** 이 환경). 최종 배포 형태는 H단계 미결
- 고도몰 키는 **채팅으로 받지 않는다.** Preview 환경변수 등록 → 검증 → Production 등록 순서
- 임시 파일은 시스템 temp의 세션 스크래치패드에 만들고 저장소에 남기지 않는다
