# AGENTS.md — 이 저장소에서 일하는 AI 작업자 지침

이 파일은 **Codex**의 세션 시작 지침이다. Claude는 `CLAUDE.md`를 읽는다.
두 파일은 **같은 governance 정본**을 가리키며, 규칙이 다르면 governance 정본이 우선한다.

## 0. 이 지침의 첫 목적

완벽한 계획을 만드는 것이 아니라, **Codex와 Claude가 다음 전체를 동일하게 이해하고 기억하며 같은 대전제에서 일하게** 하는 것이다(헌법 §0).

사용자 목적·철학·제품 콘셉트 · 전체 구조와 기존 기능 · **기능 존재와 실무 완주의 차이** · 현재 상태와 과거 결정 · 미결정 사항 · 보존할 자산과 이유 · **실제 관측·보고·추측·미검증의 구분**

컨텍스트가 길어지거나 세션이 바뀌거나 중간에 다른 작업이 끼어들어도 이 이해가 흔들리지 않아야 한다.

---

## 1. 시작 잠금 — 아래를 하기 전에는 어떤 작업도 착수하지 않는다

**필수 읽기 순서 (4종, 이 순서대로)**

1. `docs/governance/CONSTITUTION.md` — 무엇을 해도 되고 안 되는가
2. `docs/governance/MASTER_PLAN.md` — 지금 어느 단계이고 다음 한 작업은 무엇인가
3. `docs/governance/CURRENT_STATE.md` — 지금 저장소·배포·데이터의 실제 상태
4. `docs/governance/DECISIONS.md` — 이미 정해진 것을 다시 뒤집지 않기 위해

**시작 잠금**: 작업 시작 보고의 **첫 줄**에 `MASTER_PLAN.md §2`의 **현재 단계와 다음 한 작업을 인용**한다.
인용하지 못하면 문서를 읽지 않은 것으로 간주하고 **착수하지 않는다**(헌법 §15).

**요청이 현재 단계 밖이면**: 착수 전에 그 사실을 사용자에게 먼저 말한다. 임의로 단계를 건너뛰지 않는다.

## 2. 직접 Git 관측 — 문서를 믿지 말고 저장소를 본다

매 작업 시작 시 **직접 실행해서** 확인한다. 다른 문서나 이전 보고서의 값을 인용하지 않는다.

```
git rev-parse --abbrev-ref HEAD
git rev-parse HEAD
git rev-parse main origin/main
git status --porcelain
```

`CURRENT_STATE.md`의 값과 다르면 **문서가 낡은 것**이다. 실제 관측값을 보고하고 문서를 갱신한다.

## 3. 완료보고 독립검증 의무

Claude(또는 어떤 작업자)의 완료 보고를 **그대로 인정하지 않는다**(헌법 §12).

검증 방법:
- 보고에 적힌 **재현 명령을 직접 실행**하고 출력을 확인한다
- 파일:행 주장은 **해당 파일을 직접 열어** 확인한다
- "무회귀"라는 주장은 `npm test` 실행 결과로만 인정한다
- 화면 기능은 **실제 진입 경로**로 확인한다(코드 존재 ≠ 실무 작동, 헌법 §6)

검증하지 못한 항목은 보고서에 **"미검증"**으로 표기한다.

### 검증 일관성 (헌법 §10)

같은 대상을 다시 검증한 결과가 이전과 다르면 **결론부터 번복하지 않는다.**
**범위·명령·커밋·시점·환경의 차이를 먼저 기록하고 원인을 설명한다.**
확인 범위가 달랐다면 새 사실로 포장하지 않고 **"이전 주장의 범위 오류"**로 기록한다.

사용자가 견딜 수 없는 것은 버그가 아니라 ① 했던 말을 근거 없이 번복하는 것 ② 같은 작업을 다시 검증할 때 결과가 달라지는 것 ③ 과거 결정과 초기 콘셉트를 잊는 것 ④ 눈앞의 문제를 수습하다 전체 목적을 잃는 것이다.

## 4. 사용자 보고 형식 (헌법 §14)

기술 증거보다 **먼저** 다음 4가지를 쉬운 말로 쓴다.

1. 실제로 끝난 것
2. 사용자가 체감할 변화
3. 남은 것과 이번 범위 밖인 것
4. 사용자가 결정해야 할 것이 있는지

금지: 분모 없는 진행률 · 자동검사를 육안검증으로 표현 · Preview 결과를 Production 완료로 표현 · 보고서 주장만 재인용 · 새 문제를 발견할 때마다 전체 완료를 다시 열기

## 5. 하지 않는 것 (헌법 §13)

사용자의 **명시적 승인 없이** 하지 않는다:

- main 병합 · Production 배포 · 브랜치 삭제
- 데이터 삭제 · 환경변수 변경
- 외부 WRITE(고도몰 등록·고객 답글 발송·SNS 게시) · 결제
- `amend` · `rebase` · `force push`

**침묵을 승인으로 해석하지 않는다**(헌법 §11).

## 6. 범위 규율 · 안정화의 뜻 (헌법 §8·§9)

**안정화는 결함을 미리 다 없애는 것이 아니다.** 기본이 작동하고, 이후 패치·수정·기능 추가·기술 교체에도 전체가 연쇄로 무너지지 않게 만드는 것이다. **완벽을 이유로 실제 사용을 미루지 않는다.**

- 한 번에 **하나의 종료조건 또는 하나의 결함**만 다룬다
- 착수 전에 성공 기준과 범위를 고정한다
- 도중 발견은 **현재 종료조건을 직접 막을 때만** 포함한다
- 나머지는 `MASTER_PLAN.md §14 후속 대장`에 기록하고 넘어간다
- 같은 근본 원인을 새 이름으로 계속 늘리지 않는다

### Patch / 단계 작업 / 구조 패치의 구분

| 구분 | 무엇 | 어디서 |
|---|---|---|
| **Patch** | 완료된 기능에서 **실제 사용 중** 발견된 결함 | 단계를 다시 열지 않고 Patch 트랙 |
| **단계 작업** | 아직 만들지 않은 기능 | 해당 단계 |
| **구조 패치** | 핵심 경계(회귀 게이트·공통 데이터 입구·저장 경계·actor/executor·TeamId 정본)를 바꾸는 수정 | `구조 패치`로 표시하고 **사용자에게 영향 보고** |

모든 Patch는 관련 검사와 `npm test`를 통과하고, **수정 이유·영향·검증 결과를 기록**한다.
**과거 단계를 완료하지 않은 것처럼 다시 열지 않는다.**

### 용어 (혼동 방지)

- **B-use의 완주** = `핵심 업무 흐름 시나리오 완주`
- **F의 완주** = `팀별 대표 AI 업무 완주`

**B 완료와 오픈 최소 구성 완료는 다른 것이다.** 미룬 항목은 완료로 덮지 않고 후속으로 명시적으로 재분류한다.

### 착수 전 능동 판단 — 제안은 넓게, 구현은 좁게

사용자가 목적·아이디어·바람을 말하면 곧바로 구현 지시로 바꾸지 않는다. 먼저 **현재 코드·배포·결정·후속 계획을 직접 확인**하고 다음을 짧게 제시한다.

1. **판단** — 지금 하는 것이 타당한지, 왜 필요한지, 뒤 단계와 합친 뒤 하는 편이 중복 작업을 줄이는지
2. **확인된 대안** — 새 기능·구조 선택·직접 개발 여부를 결정하는 작업이라면 공식 문서와 현재 정보를 확인해 상용 서비스·API·MCP·플러그인·검증된 오픈소스를 연결만 해서 해결할 수 있는지
3. **추천** — 비용·시간·현재 시스템과의 적합성을 비교한 가장 단순한 수순. 방향을 바꿀 중요한 선택이 남으면 구현 전에 사용자에게 묻는다
4. **이번 한 작업** — 선택이 끝나면 하나의 종료조건만 구현한다

외부 대안은 존재와 적용 가능성을 확인한 것만 말하고, 사실과 추론을 구분한다. 사소한 결함 Patch마다 시장조사를 벌이지 않는다. **작업 전 판단 절차 자체를 큰 보고서나 새 개발 과제로 만들지 않는다.**

완벽한 최종형을 미리 만들지 않는다. 먼저 실제로 쓸 수 있는 완성을 만들고 사용자의 손검수에서 확인된 결함을 핀포인트로 고친다. 인접 기능·선제 최적화·광범위 안정화는 현재 종료조건을 직접 막거나 지금 하지 않으면 명백히 중복 구현되는 경우가 아니면 후속으로 미룬다.

## 7. Codex의 상시 확인 항목

- **고도몰 키 상태**: B 진행 중 매 단계 보고에 한 줄로 확인한다. 키가 확보된 사실이 확인되면 **사용자가 언급하지 않아도** 먼저 C 착수를 제안한다(`MASTER_PLAN.md §6`)
- **단계 종료 = 문서 갱신**: `MASTER_PLAN.md` 갱신 없이 단계 완료를 선언하지 않는다
- **사업 기준선**: 2026년 11월 말 내부 오픈 목표 / 12월 마지노선. 마감이 다가와도 **완료 조건을 낮추지 않고 범위를 조정**한다(헌법 §13)
