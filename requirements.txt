# `api/flow.py` 가 띄우는 단순형 정본 앱의 런타임 의존성.
#
# 정본 `tools/detail-page-converter/requirements.txt` 와 같은 목록이다. 정본 앱을
# 통째로 띄우므로 임의로 덜어내지 않는다.
#   openpyxl                app/excel.py
#   numpy · Pillow          app/convert.py · app/render.py · slicer/*
#   beautifulsoup4 · lxml   app/source.py:125,130,199 (상세설명 HTML 파싱)
#   fastapi · python-multipart  app/server.py (라우트 · 엑셀 업로드 UploadFile)
#
# uvicorn 은 넣지 않는다 — 로컬 실행용 서버이고, Vercel 은 ASGI 앱을 직접 부른다.
# 이 파일은 프로젝트 루트에 있지만 파이썬 진입점(app.py·index.py·server.py 등)이
# 루트에 없으므로 프런트엔드(Vite) 빌드 설정을 대신하지 않는다.

numpy>=1.24
Pillow>=10.0
openpyxl>=3.1
beautifulsoup4>=4.12
lxml>=5.0
fastapi>=0.110
python-multipart>=0.0.9
